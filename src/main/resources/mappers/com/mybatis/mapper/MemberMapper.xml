<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.MemberMapper">

	<insert id="insert">
		INSERT INTO MEMBER VALUES(MEMBER_NO_SEQ.NEXTVAL, #{memberID}, #{memberPW}, #{memberName})
	</insert>

	<select id="selectByMemberNo"
		resultType="com.mybatis.domain.Member">
		SELECT * FROM MEMBER WHERE MEMBER_NO = #{memberNo}
	</select>

	<update id="update">
		UPDATE MEMBER SET MEMBER_ID = #{memberID}, MEMBER_PW = #{memberPW} , MEMBER_NAME= #{memberName} WHERE MEMBER_NO = #{memberNo}
	</update>

	<delete id="delete">
		DELETE FROM MEMBER WHERE MEMBER_NO = #{memberNo}
	</delete>

	<select id="selectAll"
		resultType="com.mybatis.domain.Member">
		<![CDATA[
			SELECT * FROM MEMBER WHERE MEMBER_NO > 0 ORDER BY MEMBER_NO DESC
		]]>
	</select>
</mapper>